/target/

# Generated scene pack — produced from the TypeScript composer via
# `(cd ../blast-stress-solver && npm run build:ts && npm run generate:high-rise)`.
# Not committed (large, regenerated); the demo/tests load it from disk at runtime.
/assets/scenes/high-rise.json
